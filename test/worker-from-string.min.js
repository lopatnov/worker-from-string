(function (global, factory) {
    typeof exports === 'object' && typeof module !== 'undefined' ? module.exports = factory() :
    typeof define === 'function' && define.amd ? define(factory) :
    (global = typeof globalThis !== 'undefined' ? globalThis : global || self, global.workerFromString = factory());
})(this, (function () { 'use strict';

    function workerFromString(...e){let r,t=e.join("");try{r=new Blob([t],{type:"application/javascript"});}catch(e){let o=new(BlobBuilder||WebKitBlobBuilder||MozBlobBuilder);o.append(t),r=o.getBlob();}return new Worker(URL.createObjectURL(r))}

    return workerFromString;

}));
